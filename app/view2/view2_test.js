'use strict';

describe('version.view2 module', function() {

  beforeEach(module('productManagement.view2'));

  describe('productManagement controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});